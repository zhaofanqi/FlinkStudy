锁
并发：同一时刻多个线程抢夺同一个资源
并行：同一时刻多个线程处理各自任务
进程：一个程序就是一个进程
线程：轻量级的进程，一个进程可以有多个线程。是大多数操作系统进行时序调度时的基本单元
管程： 1 Monitor（监视器）其实是一种同步机制，保证同一时间，只能有一个线程访问被保护的数据和代码
      2 jvm3中提到：执行线程拥有了管程之后才能执行方法，方法执行完以后释放管程，在执行期间，其他线程无法获取管程
用户线程：完成程序需要完成的业务工作的线程（俗称工作线程），默认就是用户线程。
守护线程：服务线程，是一种特殊的线程，用于服务其他线程的线程，当守护的线程结束时，他也就结束了。例如 垃圾回收线程